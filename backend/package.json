{
  "name": "nakama-tictactoe-backend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npx esbuild src/main.ts --outfile=modules/index.js --target=es5 --supported:const-and-let=true --supported:arrow=true --supported:for-of=false --supported:destructuring=false && sed -i.bak '/\"use strict\";/d' modules/index.js && rm -f modules/index.js.bak"
  },
  "devDependencies": {
    "esbuild": "^0.28.0",
    "nakama-runtime": "github:heroiclabs/nakama-common",
    "tslib": "^2.6.0",
    "typescript": "^5.3.0"
  }
}
